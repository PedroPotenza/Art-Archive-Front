import { atom } from "jotai";

export const selectedFilters = atom("Red");

export const excludedFilters = atom("Japan");

export const isFilterOpenAtom = atom(false);

export const colorsAtom = atom([]);
